<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cmsc436.semesterproject.localapparel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name="cmsc436.semesterproject.localapparel.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="cmsc436.semesterproject.localapparel.LoginActivity" />
        <activity android:name="cmsc436.semesterproject.localapparel.RegistrationActivity" />
        <activity android:name="cmsc436.semesterproject.localapparel.FeedActivity" />
        <activity android:name="cmsc436.semesterproject.localapparel.AddItemActivity" />
    </application>

</manifest>